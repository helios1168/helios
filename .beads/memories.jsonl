{"_type":"memory","key":"2026-09-13-decision-user-helios-implementer-models","value":"2026-09-13 decision (user): helios implementer models. Free opencode/muse-spark-1.3-contributor-free for H2a2 and H2b (literal parsing work). opencode-go/gpt-5.6-luna for H2c (sessions, tmux, inbox) and the Wave 3 merge (H3c) and control/unit run (H3e) beads, via bead metadata model. Codex (Terra) not used: its bypass mode breaks the kit-repo-only data rule."}
{"_type":"memory","key":"2026-09-13-decision-user-keep-opencode-plugins","value":"2026-09-13 decision (user): keep .opencode/plugins/helios-events.js as the opencode event source. opencode plugins must be JS/TS; no Python SSE listener on opencode serve."}
{"_type":"memory","key":"2026-09-13-git-a-literal-pathspec-matching","value":"2026-09-13 git: a ':(literal)' pathspec matching nothing makes git add fail (128) and git commit fail. For add, filter to paths on disk or in the index; for commit, select with git diff --cached --name-only --no-renames -z HEAD, which lists git rm and git mv sources that commit accepts."}
{"_type":"memory","key":"2026-09-13-helios-code-verify-fuzz-fixed","value":"2026-09-13 helios code-verify: fuzz fixed-point or greedy loops under a per-call timeout, attack section-level config types, and test markdown lookups against units with look-alike subheadings; each found a bug the green suite missed."}
{"_type":"memory","key":"2026-09-13-helios-code-verify-test-process","value":"2026-09-13 helios code-verify: test process cleanup with a grandchild that ignores INT and TERM and outlives the leader; SIGINT storms alone missed a leaked group. Exercise the native report channel by patching FakeHarness.parse to return structured; simulate agent git actions with a bash -c wrapper around the fake argv."}
{"_type":"memory","key":"2026-09-13-helios-delegation-the-free-opencode","value":"2026-09-13 helios delegation: the free opencode model implements the SPEC letter; every H2a1 refutation traced to an undefined edge (heading and fence shape, glob overlap and segment rules, cap algorithm, recovery states). Define parsing, glob and termination rules exactly in SPEC before filing a bead."}
{"_type":"memory","key":"2026-09-13-orchestrator-never-hold-an-enterworktree","value":"2026-09-13 orchestrator: never hold an EnterWorktree session while a background verifier subagent runs; the isolation guard then refuses every Bash call of the subagent. Do SPEC edits between verifier runs, or verify first."}
{"_type":"memory","key":"2026-09-13-python-path-read-text-raises","value":"2026-09-13 python: Path.read_text raises UnicodeDecodeError (a ValueError, not OSError), and str.splitlines also splits on U+2028, U+2029, U+0085 and \\x0b\\x0c\\x1c-\\x1e, which JSON allows raw inside strings. JSONL parsers read bytes and split on b'\\n' only."}
{"_type":"memory","key":"2026-09-14-bd-1-2-2-quirks","value":"2026-09-14 bd 1.2.2 quirks: bd show matches partial ids (check the returned id), bd memories --json mixes a schema_version key into the value map, bd remember echoes a value truncated at about 77 bytes that can split a UTF-8 character, and the memory value column rejects 65536 bytes or more."}
{"_type":"memory","key":"2026-09-14-codex-output-schema-strict","value":"2026-09-14 codex exec --output-schema (codex-cli 0.154.0) uses OpenAI structured outputs in strict mode: every object must list all properties in required and set additionalProperties false, optional fields must be nullable instead of omitted, and pydantic model_json_schema output is rejected with 400 invalid_json_schema. Stub-binary adapter tests cannot catch this; only a live run does. Also: a codex profile with the Serena MCP creates .serena/ in the worktree, so gitignore .serena/ or ownership fails."}
{"_type":"memory","key":"2026-09-14-decision-user-helios-review-and-smoke","value":"2026-09-14 decision (user): after opencode hit its usage limit, implementation goes to Claude Sonnet subagents and verification stays with Opus code-verify subagents (no Codex reviewer). The end-of-Wave-2 live smoke tests run on codex and agy only for now. The opencode server was shut down. The orchestrator's interim decisions (memory injection moved to hel-dgl, claim identity and name grammar, 2 s collection bound, unit run stop order, helios: prefix on every stderr line) stand."}
{"_type":"memory","key":"2026-09-14-decision-user-opencode-is-near","value":"2026-09-14 decision (user): opencode is near its session limits; remaining helios implementation turns (fix rounds for hel-35y, hel-oj1, hel-awc, hel-g5b, hel-c90, hel-ukz, hel-6ks and the undispatched H2c2 hel-dgl) go to Claude Sonnet general-purpose subagents working in the existing bead worktrees, using tmp/sonnet-worker-template.md. Verification stays with Opus code-verify subagents."}
{"_type":"memory","key":"2026-09-14-helios-bd-gate-list-null","value":"2026-09-14 bd 1.2.2: bd gate list --json -n 0 prints null (not []) when no gate is open; helios.gates treats null as no open gates (fixed after the Wave 3 end-to-end run). A test hub needs the SPEC 2.2 .gitignore (.helios/, .claude/worktrees/) or the ownership check fails with exit 5."}
{"_type":"memory","key":"2026-09-14-helios-claim-identity-lesson","value":"2026-09-14 helios lesson: (module, __qualname__, co_firstlineno) is not a unique callable identity. One-line lambdas, factories, functools.wraps pairs and exec blocks collide, and callable instances or functools.partial lack __code__ or a true __module__. Key registrations by (calling module frame, declared name) within one fresh import pass instead."}
{"_type":"memory","key":"2026-09-14-helios-code-verify-attack-every","value":"2026-09-14 helios code-verify: attack every pid field with 0, -1, true and a string (os.kill(0,0) succeeds and killpg(0) signals the caller's own group), every JSONL reader with non-object lines and invalid UTF-8, every timestamp without an offset, and every exec'd temp module with a dataclass (module must be in sys.modules). On macOS killpg of a zombie group leader raises PermissionError, not ProcessLookupError."}
{"_type":"memory","key":"2026-09-14-helios-live-smoke-result","value":"2026-09-14 helios live smoke PASSED on codex and agy (synthetic hub jobs/dd6f7188/tmp/smoke/hub, main at 666e0fd or later). codex s-hqj: exit 0, completed, report done, output_commit bfca9a9, test and ownership checks passed, bead closed, 108 s. agy s-yub: exit 0, same result, 55 s. Required fixes found by the smoke: hel-aky strict agent-report schema for codex, and gitignore .serena/ in hubs where a codex profile runs the Serena MCP. helios run --again rebuilds the worktree from the attempt's recorded base commit, not current main."}
{"_type":"memory","key":"2026-09-14-helios-merge-crash-matrix-lesson","value":"2026-09-14 helios merge verification: a crash matrix where main starts level with the branch never exercises code that depends on the rebase rewriting HEAD; always build one with main ahead. A 'continuing attempt' shortcut that skips verified-commit checks lets hand commits merge after any earlier failed attempt; compare rebased commits by ordered git patch-id instead."}
{"_type":"memory","key":"2026-09-14-helios-orchestrator-checkpoint","value":"2026-09-16 helios orchestrator checkpoint. main at 0a5d448, no worktrees, working tree clean apart from .DS_Store files and the bd export. The repository still has no git remote, so every commit exists only on this disk.\n\nThe owner closed the open decisions on 2026-09-16 and SPEC section 18 records them: B is an ordered list of named checks; F, ACP dispatch, declined because claude, codex and agy already expose structured JSON; H, Conductor, declined, native CLIs plus tmux stay; K is Langfuse over OTLP/HTTP with export best effort; M, Graphiti, declined for now; O, Lean, deferred until the math-verify port.\n\nThree waves shipped. Wave 1, SPEC batch 12, at 5b62e1e: section 5 gained [[project.check]], sections 7.1 step 9 and 12 step 4 run the configured checks, the section 12 markers stay tested and test-failed whatever the checks are named, and a new section 21 Telemetry sits after section 20 so nothing was renumbered. Wave 2, hel-7br, at e446cb1: CheckEntry and effective_checks, six refusal strings verbatim, sugar mapping project.test to when=merge and project.typecheck to when=both so default behaviour is unchanged. Wave 3, hel-xm0, at 0a5d448: src/helios/telemetry.py is the only module importing OpenTelemetry, export runs in a daemon thread joined with timeout_s, and the OTel packages are a telemetry extra rather than core dependencies. Suite 1264 passing.\n\nTwo lessons worth keeping. BatchSpanProcessor.force_flush reports success even when the exporter failed, so check SpanExportResult directly or every failed export is invisible. And a commented array-of-tables example in templates/workflow.toml must sit after every plain key of the table it extends, because uncommenting one placed mid-table silently swallows every key below it.\n\nOpen beads: hel-f1g, P1, helios run prints a traceback for a bad workflow.toml instead of helios: message exit 2, because cli.py main wraps nothing and commands/run.py loads config outside any handler. hel-5eh, P2, two SPEC section 21 gaps, a failed merge export has no note target and the listed child order contradicts the section 7.1 step order. hel-xev, P2, nothing in the suite parses the shipped templates/workflow.toml."}
{"_type":"memory","key":"2026-09-14-helios-patch-id-stable-lesson","value":"2026-09-15 lesson (hel-oj1 rounds 4 to 7): proving a worktree HEAD is the verified commit rebased. No diff comparison works. git patch-id --stable strips whitespace; --verbatim ignores bytes after a NUL and mode lines; git show output bends to git config (diff.submodule=log splits the patch, ignoreSubmodules=all hides pointer changes). A SHA-256 over pinned-flag git show bytes fixes those but stays position-blind once hunk headers are normalized away, so an edit moved to an identical block compares equal. Replay instead of compare, and replay commit by commit: a single squashed git merge-tree of the whole range loses per-commit rename detection, so a branch that renames a file then rewrites it while main edits the old path rebases cleanly yet replays as a modify/delete conflict, and helios then refuses the HEAD it rebased itself on every rerun. Final rule: cursor starts at merge-base(HEAD, main); for each commit c of output_base..output_commit run git merge-tree --write-tree --merge-base=\u003cc^\u003e \u003ccursor\u003e \u003cc\u003e, exit 1 refuses, then cursor = commit-tree of the replayed tree; the final cursor tree must equal HEAD's tree. A missing or empty merge-base refuses (exit 2), and every git call takes --no-replace-objects. Probes: tmp/fp-probe, tmp/replay-probe, tmp/seq-probe. Probe an equivalence rule against position, renames across commits, config, NUL, mode and submodule variants before writing it into the SPEC."}
{"_type":"memory","key":"2026-09-14-helios-ps-lstart-locale-lesson","value":"2026-09-14 lesson (hel-64i r1): macOS ps -o lstart= follows LC_ALL, LANG, LC_TIME and TZ, and tmux windows keep the tmux server's environment, so any text written by one helios process and compared by another must be produced in a fixed environment (LC_ALL=C, TZ=UTC0). macOS ps also reports lstart for zombie processes."}
{"_type":"memory","key":"2026-09-14-helios-spec-pending-clarifications-to","value":"2026-09-15 helios SPEC pending clarifications: batches 5 to 11 applied (f0816d1, 0d4c76b, 29c61f6, 58d4211, e0edf3e, 792be5a, 915d945). Batch 11: the gitignore preflight follows the configured project.runs and project.worktrees directories instead of the literal .helios/ and .claude/worktrees/, probing \u003cruns\u003e/probe and \u003cworktrees\u003e/probe and naming the configured directory in the refusal. Nothing pending. hel-4xv implements batch 11; record its outcome here."}
{"_type":"memory","key":"2026-09-14-helios-verify-severity-rule","value":"2026-09-14 helios orchestration: after round 3 or later, code-verify runs use a severity rule. REFUTED only for a defect on a SPEC or decision path that gives a wrong result, a crash or traceback on realistic input, data loss, or a hang. Robustness edges needing hand-corrupted state, hostile environments, or SPEC-open behavior are listed as follow-ups with a one-line fix and filed as beads, not refutations. Keeps verify rounds converging."}
{"_type":"memory","key":"2026-09-14-helios-workers-luna-h2c1-turn","value":"2026-09-14 helios workers: Luna H2c1 turn 1 said pyright is not installed and shipped 2 tests for 5 acceptance items; tell every worker the exact uvx pyright --pythonpath .venv/bin/python command and gate a bead on per-item tests before spending a verifier."}
{"_type":"memory","key":"2026-09-14-helios-worktree-absolute-path-lesson","value":"2026-09-14 lesson: create bead worktrees with absolute paths (git -C \u003chub\u003e worktree add \u003chub\u003e/.claude/worktrees/\u003cid\u003e). A relative path run from inside another worktree nested hel-64i under hel-951, and removing hel-951 deleted the nested directory while its registration and lock stayed. Recovery: git worktree unlock \u003cold path\u003e, git worktree prune, git branch -D the empty branch, recreate."}
{"_type":"memory","key":"2026-09-15-helios-e2e-w3b","value":"2026-09-15 helios Wave 3 end-to-end: the first rerun (tmp/e2e-w3b/results.md, snapshot 84d1957) was E2E FAIL on one P1 defect, helios merge refusing every real verify envelope as stale, because merge._default_hashes read bead.metadata['input_hashes'] which nothing writes and _verify_evidence hashed the impl bead instead of the verify bead. Unit tests missed it because they inject the hash function, and six of them passed only because both sides of the comparison were empty. Fixed as hel-z3b at 1817cdc (merge recomputes the verify bead's own hashes through run.recompute_input_hashes). The merge recheck on that snapshot (tmp/e2e-merge/results.md) is MERGE E2E: PASS across all five checks: a real unit run, dry-run, a real merge after main moved, a stale envelope after a unit file changed, and a hand commit on a verified worktree. Lesson: cover at least one command path end to end with no injected collaborators, or an always-empty comparison looks green. The 27563b4 gate fix is confirmed. SPEC gaps from the rerun are now closed: the preflight prefix (hel-gee, e0edf3e) and the merge --dry-run text (SPEC 792be5a, code in hel-1b2)."}
{"_type":"memory","key":"2026-09-15-helios-live-smoke-w3","value":"2026-09-15 helios Wave 3 live smoke PASSED on codex and agy against snapshot 0d72702 (hub and report in jobs/dd6f7188/tmp/smoke-w3). Full chain for the first time: unit new, unit run --until verify-code with codex implementing and agy verifying through real CLI calls, ps and stop and resume against a live codex exec process, then a real merge after main moved, plus gate and learned. helios merge worked on the first attempt with no workaround against real agent work, which closes the staleness defect the synthetic e2e found (fixed at 1817cdc). One SPEC GAP found and filed: preflight hardcodes the gitignore probe paths instead of reading project.worktrees and project.runs, so a hub with a non-default worktrees directory is refused over a directory it never uses. One SETUP item: a hub must also gitignore whatever the harness itself writes, such as codex's .serena/ onboarding files and __pycache__, or the ownership check correctly rejects them with exit 5."}
{"_type":"memory","key":"2026-09-15-helios-worker-suite-and-bg","value":"2026-09-15 helios workers: the full suite now runs about 10 to 13 minutes (1050+ tests, heavy git and subprocess fixtures), so tell every worker and verifier to split it by test file with an explicit 600000 ms Bash timeout, and never to use run_in_background or Monitor: a background completion notice never reaches a subagent, and the agent stops mid-task waiting for it (the hel-oj1 round 6 verifier did exactly that and needed a SendMessage to resume). Worktrees created fresh have no .venv; the worker runs uv sync first, and every pytest call needs PYTHONPATH=\u003cworktree\u003e/src or helios imports from the main repo."}
{"_type":"memory","key":"2026-09-16-background-job-worktree-guard","value":"2026-09-16 Claude Code background jobs: the orchestrator cannot edit the shared checkout at all. Edit and Write against the hub are refused until the session isolates, and the refusal names EnterWorktree. But once the session is isolated by EnterWorktree, every git command is refused too, including read-only ones such as git status, because the rtk rewrite hides which git runs and the worktree guard will not allow a git it cannot attribute. Those two rules together mean neither the hub nor an isolated session can both edit and commit.\n\nThe pattern that works, in this order: create the worktree from the hub with plain git, git worktree add .claude/worktrees/\u003cid\u003e -b worktree-\u003cid\u003e followed by git worktree lock --reason keep; call EnterWorktree with the path, never with a name; make every edit; call ExitWorktree with action keep, which returns the session to the hub and leaves the branch and directory intact; then commit and merge from the hub, where git works normally. Writing the commit message to a file outside the repository and using git commit -F avoids quoting problems with multi-line messages.\n\nThis cost six refused edits and four refused git calls before it was understood. Subagents are unaffected: they never call EnterWorktree, so they edit and commit inside their worktree normally."}
{"_type":"memory","key":"2026-09-17-helios-git-remote-and-bead-backup","value":"2026-09-17 helios backup: origin is git@github.com:helios1168/helios.git over SSH, pushed with the deploy_td key which authenticates as the user helios1168. A second bare mirror sits at /Users/Shared/sv-ntlee/backups/helios.git under the remote name local, kept off the name origin so bd never targets it. Bead data is not carried by a plain git push: it lives in .beads/embeddeddolt, which .gitignore excludes. Two committed exports carry it instead. .beads/issues.jsonl holds issues only and bd keeps it fresh automatically, because export.auto and export.git-add are now true in .beads/config.yaml. .beads/memories.jsonl holds the memories and has no automatic refresh, because auto-export scope excludes memories and its shrink guard refuses to overwrite a file holding records outside that scope; refresh it by hand after writing a memory with: bd export --include-memories -o /tmp/full.jsonl then grep '\"_type\":\"memory\"' /tmp/full.jsonl \u003e .beads/memories.jsonl. bd hooks are installed via core.hooksPath pointing at .beads/hooks, not .git/hooks, so checking .git/hooks alone wrongly suggests none are installed; the hooks are not the export mechanism, since bd skips auto-export while running as a git hook."}
{"_type":"memory","key":"2026-09-18-helios-config-refusal-and-span-order","value":"Configuration refusals and telemetry span order in helios, closing hel-f1g, hel-xev and hel-5eh.\n\nconfig.ConfigError lives in src/helios/config.py and subclasses ValueError. load() validates in _load_file and wraps every TypeError or ValueError from it in ConfigError, so no caller has to know which builtin a given check raises. cli.main() holds the one handler that prints 'helios: \u003cmessage\u003e' and returns 2; the ten command modules with their own handler catch ConfigError first through ValueError and print the identical line, so they never reach the shared handler. Do not add a copy of that handler to a new command module: loading configuration outside a handler is now correct, because cli.main covers it. Loader tests in tests/test_config.py expect config.ConfigError, not TypeError; all eleven such sites went through cfg.load.\n\ntests/test_templates.py loads templates/workflow.toml through the real loader and compares field by field with Config(hub). The harness dict is compared per entry rather than against default.harness, because a bare Config names no harness while the template names claude, codex, opencode and agy as empty tables. A guard test damages the template two ways and asserts each is refused. Damage the template by replacing a line, never by appending a table, since appending duplicates an existing table and raises a TOML parse error instead of the unknown key or wrong type the test claims to check.\n\nSPEC section 21 lists the attempt children as launch, validate, check.\u003cname\u003e, commit. That is the order of section 7.1, which parses and classifies at step 8, runs the checks at step 9 and commits at step 10, so the emitted order agrees with the span timestamps. _attempt_trace in src/helios/run.py builds them in that order and tests/test_telemetry.py asserts both the names and that the start timestamps rise with them. A failed or skipped merge export is silent by specification: a merge has no attempt record, helios merge has a fixed stderr and exit code contract from section 12, and its bead comments are the step markers the replay rule of section 7.5 reads back, so neither can carry a telemetry note.\n\nThe full suite is slow: 1283 tests, about 10 minutes 35 seconds. Run it in the background and wait on the summary line, never in the foreground with a 120 second tool timeout."}
{"_type":"memory","key":"2026-09-18-helios-stage-set-design","value":"Design for making helios stages configurable, so general purpose use is first class and the research workflow is one declared stage set among many. Decided with the owner on 2026-09-18: stages carried as config data, one package layered into a core and a research pack, and only the research set shipped.\n\nTHE DECLARATION. workflow.toml gains an array of tables:\n\n[[stage]]\nid = \"impl\"\nrequires = [\"files\", \"test\"]   # subset of files, test, unit, parent, model\nauthor = \"implement\"            # a key in [agents]\ngate = \"report\"                 # report | verdict | none\nownership = \"files\"             # files | artifacts | none\nscaffold = true                  # false keeps the stage out of helios unit new\n\nA verify stage names what it checks rather than being recognised by its id prefix:\n\n[[stage]]\nid = \"verify-code\"\nverifies = \"impl\"\nrequires = [\"unit\", \"parent\"]\nauthor = \"other\"\ngate = \"verdict\"\nownership = \"artifacts\"\n\nverifies replaces both VERIFY_STAGES and the parent_stage heuristic that searched backwards for the nearest earlier non-verify stage. The parent bead and the worktree base commit come from the stage named there. Declaration order is stage order.\n\nLAYERING. New core module src/helios/stageset.py holds StageSpec and StageSet: ids in declaration order, index, lookup, the stages whose gate is verdict, and parent_of. The ten research stages move out of code into a shipped data file loaded when a hub declares no [[stage]], so an existing hub behaves as before. No core module may name a research stage; that is the layer boundary and a test should assert it by grepping the core modules for the ten ids.\n\nCONSEQUENCES ACCEPTED. [agents] becomes open keyed: AgentsConfig carries roles as a dict plus verify_order, keeping the six documented defaults. A stage's author must resolve to a declared role key, so a typo is still refused and verify_code, verify_math and verify_validate stop being special names in the loader. ownership gains the mode none for general tasks with no file list; the unconditional rejections stay unconditional, meaning .git, the memory export directory and anything matching project.confidential. control.stop_at keeps its research default of frame, model and report only while the hub declares no stages; a hub that declares its own gets an empty default and every id it lists must exist. --until and control.until validate against the declared ids.\n\nCOUPLING POINTS FOUND 2026-09-18, three independent copies of the stage list. src/helios/stages.py lines 7 to 20 (STAGES, VERIFY_STAGES, parent_stage). src/helios/config.py line 29 (_VERIFY_KINDS) and lines 425 to 450 (spec_for_kind's five branches, harness_for_kind, is_verify_kind). src/helios/units.py lines 31 to 56 (STAGE_ORDER without remember, _VERIFY_STAGES, _NEEDS_FILES, _VERIFY_AGENT_KEY, and _USABLE_AUTHOR_RE which hardcodes the four harness names), lines 154 to 187, 290 to 333 and 383 to 456.\n\nKind branches to replace with stage fields: ownership.py 139 and 157; run.py 658, 737, 1226 and 1562; beads.py 250 should_close; preflight.py 83, 88 and 100; control.py 25, 159, 238 and 242; learned.py 34.\n\nLabel form stays kind:\u003cstage\u003e and unit:\u003cunit\u003e; skills/\u003ckind\u003e/SKILL.md stays required per stage id, checked at run.py 780 to 783 and read at prompt.py 150.\n\nTEST WEIGHT. Roughly 180 tests assert stage names: tests/test_run_fake.py 77 of 93, tests/test_unit_new.py 34 of 48, tests/test_control.py 33 of 49, tests/test_learned.py 18 of 24, tests/test_resume.py 14 of 29, tests/test_stages.py 5 of 5. test_stages.py parses the SPEC section 3 table and asserts parity with STAGES; keep that parity check and point it at the shipped research declaration instead.\n\nSPEC WORK. Section 3 becomes the stage declaration schema with the shipped research set as a subsection. Section 5 gains the [[stage]] table and the open keyed [agents]. Section 7.1 states the requires checks generically, section 10.2 drives unit new from the declared set and the scaffold flag, section 11 line 1012 stops pointing at helios.stages.STAGES and points at the configured set. Section 11's five stop checks key on gate, not on kind."}
{"_type":"memory","key":"2026-09-18-helios-stage-set-outcome","value":"Outcome of making helios stages configurable, finished 2026-09-18 across eight beads: hel-i5l, hel-8p3, hel-j6s, hel-263, hel-4e2, hel-961, hel-cm9, hel-2in and hel-fq0. The design is in 2026-09-18-helios-stage-set-design; this memory records what the work actually settled.\n\nWHAT SHIPPED. src/helios/stages.py is deleted. src/helios/stageset.py holds StageSpec and StageSet. src/helios/stagesets/research.toml declares the ten research stages and is loaded when a hub declares no [[stage]] entry, so an existing hub is unchanged. Every consumer reads stage fields: preflight demands what requires lists, ownership takes a mode of files, artifacts or none, run's exit code and beads.should_close follow gate, a fresh worktree starts at the parent's output_commit when the stage declares verifies, control's five stop checks key on gate, and unit new scaffolds from declaration order and the scaffold flag. Suite went from 1264 to 1327 tests, all passing.\n\nTHE DEFECT CLASS THIS WORK KEPT PRODUCING. A stage set parameter with a research default is a silent wrong answer, not a crash: a hub declaring its own stages gets judged against research stages with no error anywhere. It appeared three times, in PreflightContext, in control and learned, and in the command modules that call them. Two were caught by workers reporting the trap rather than by a test. The stage set is now a required argument in control and learned, every command module forwards config.stages, and run's preflight_errors passes it too. Do not reintroduce a default for a configured thing that has a plausible looking fallback.\n\nDECISIONS WORTH KEEPING. Bead.kind defaults to empty rather than impl, in the dataclass and in from_show, because a bead with no kind has no stage and should get the refusal an undeclared kind gets. [agents] is open keyed; a stage's author must resolve to a declared role, which is the typo defence that replaced the closed key list. The usable author check asks harness.get rather than matching a fixed list of four harness names, which makes the fake harness usable as a parent author where the old regex excluded it. unit new's files and test demands are now independent, so a stage may require one without the other.\n\nTHE LAYER TEST AND ITS ONE HOLE. tests/test_stages.py asserts no core module contains a research stage id, counting an id only when it appears as a quoted string literal equal to the id, with an allowlist of exact lines for words that are also field names, gate values, check names and telemetry span names, plus a test that fails when an allowlisted line disappears so an exemption cannot go stale. The word remember cannot be distinguished structurally, because beads.py calls bd remember as the generic memory verb and it is spelled the same as the stage; those lines are allowlisted with the limitation stated. src/helios/stageset.py is not in the core list because it defines the REQUIREMENTS, GATES and OWNERSHIP_MODES vocabularies.\n\nGENERAL PURPOSE USE. tests/test_general_purpose.py takes a hub declaring one stage named solo, with no files or test demand, ownership none and gate report, through helios run and helios merge with the fake harness. That test is the proof that a non research hub is first class. templates/workflow.toml documents [[stage]] in comments and still parses to the defaults.\n\nPROCESS NOTE. A bead that edits a command module must have that module's own test file in its test command. hel-cm9 edited commands/learned.py with a test command that did not include tests/test_learned.py, and five broken tests surfaced only at merge."}
