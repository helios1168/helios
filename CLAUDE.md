# helios

Read `AGENTS.md`. In the main session you are the orchestrator: the worker contract there
applies to bead sessions, not to you. Specification: `docs/SPEC.md`. Issues: `bd` with prefix
`hel`, one database for this repository. Tests: `uv run pytest -q`.
